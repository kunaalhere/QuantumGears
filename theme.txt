# ==============================================
# QUANTUM GEAR BOOTLOADER - theme.txt
# ==============================================

# --- Fonts ---
+ font { name = "Orbitron Bold"; file = "fonts/Orbitron-Bold.pf2" }
+ font { name = "JetBrains Mono"; file = "fonts/JetBrainsMono-Regular.pf2" }

# --- Colors ---
+ color { name = "chalk-white"; value = "#e8e8f0" }
+ color { name = "matte-black"; value = "#0a0a0a" }
+ color { name = "cyber-cyan"; value = "#00f5ff" }
+ color { name = "quantum-purple"; value = "#b44aff" }
+ color { name = "dim-gray"; value = "#3a3a4a" }

# ===== BACKGROUND (Deep matte + gears) =====
desktop-image = "assets/background.png"
desktop-color = "#0a0a0a"

# ===== GLOBAL STYLES =====
+ label {
    font = "JetBrains Mono"
    color = chalk-white
}

# ===== HEADER (Blueprint Title Block) =====
+ vbox {
    left = 5%; top = 3%; width = 90%; height = 80
    + label {
        text = "⟨Ψ| QUANTUM MECHANICAL BOOT |Ψ⟩"
        font = "Orbitron Bold"; size = 22; color = chalk-white; align = "center"
    }
    + label {
        text = "SCHEMATIC v4.2.0 // TORQUE SPEC: 1.21 GW // SECURE CHASSIS"
        size = 11; color = dim-gray; align = "center"; visible = true
    }
}

# ===== PULSING AI CORE (Live glow on blueprint) =====
+ label {
    id = "ai_status"
    text = "⚡ QUANTUM CORE: ACTIVE"
    left = 70%; top = 4%; size = 12; color = cyber-cyan; pulse = true; pulse-speed = 1200
}

# ===== MAIN BOOT MENU (Sits over the gears) =====
+ boot_menu {
    id = "boot_menu"
    left = 12%; top = 20%; width = 76%; height = 60%
    item_font = "JetBrains Mono"
    item_color = "#a0a0b0"
    selected_item_color = "#ffffff"
    item_height = 46; item_padding = 10; item_spacing = 8

    # Use the gear-tooth sliced borders
    item_pixmap_style = "assets/gear_slices/menu_*.png"
    selected_item_pixmap_style = "assets/gear_slices/menu_*.png"

    # Menu box background (slightly transparent to see gears behind)
    menu_bg_color = "#1a1a2a"
    menu_bg_alpha = 0.7
}

# ===== COUNTDOWN TIMER (Cyan Progress Bar) =====
+ vbox {
    left = 20%; top = 85%; width = 60%; height = 40
    + label { text = "AUTO-IGNITION IN"; size = 11; color = dim-gray; align = "left" }
    + label { id = "timer_value"; text = "30s"; font = "Orbitron Bold"; size = 18; color = cyber-cyan; align = "right" }
    + progress_bar {
        bar_style = "assets/progress_*.png"   # Create cyan glowing bar slices
        bar_bg_color = "#0a0a0a"
        bar_height = 4; bar_width = 100%
        value = 100
    }
}

# ===== BOTTOM KEY HINTS (Technical schematics footer) =====
+ hbox {
    left = 5%; bottom = 3%; width = 60%; height = 20
    + label { text = "[↑↓] NAV   [↵] EXECUTE   [E] OVERRIDE   [Q] SCAN"; size = 10; color = dim-gray }
}
+ hbox {
    left = 72%; bottom = 3%; width = 25%; height = 20
    + label { text = "GEAR RATIO: 0.9987   T: 15mK"; size = 10; color = quantum-purple }
}
