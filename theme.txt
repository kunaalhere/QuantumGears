# ===================================================================
# QUANTUM GEAR BOOTLOADER v4.2.0
# Theme for GRUB 2.06+
# ===================================================================

# ---- Fonts ----
+ font {
    name = "Orbitron Bold"
    file = "fonts/Orbitron-Bold.pf2"
}

+ font {
    name = "JetBrains Mono"
    file = "fonts/JetBrainsMono-Regular.pf2"
}

# ---- Colours ----
+ color {
    name = "chalk-white"
    value = "#e8e8f0"
}
+ color {
    name = "matte-black"
    value = "#0a0a0a"
}
+ color {
    name = "cyber-cyan"
    value = "#00f5ff"
}
+ color {
    name = "quantum-purple"
    value = "#b44aff"
}
+ color {
    name = "dim-gray"
    value = "#3a3a4a"
}
+ color {
    name = "menu-bg"
    value = "#1a1a2a"
}

# ---- Desktop Background ----
desktop-image = "assets/background.png"
desktop-color = "#0a0a0a"

# ---- Global Label Style ----
+ label {
    font = "JetBrains Mono"
    color = chalk-white
}

# ---- Top Header (Title Block) ----
+ vbox {
    left = 5%
    top = 3%
    width = 90%
    height = 80

    + label {
        text = "⟨Ψ| QUANTUM MECHANICAL BOOT |Ψ⟩"
        font = "Orbitron Bold"
        size = 22
        color = chalk-white
        align = "center"
    }

    + label {
        text = "SCHEMATIC v4.2.0 // TORQUE SPEC: 1.21 GW // SECURE CHASSIS"
        size = 11
        color = dim-gray
        align = "center"
    }
}

# ---- AI / Quantum Core Status (Pulsing) ----
+ label {
    id = "ai_status"
    text = "⚡ QUANTUM CORE: ACTIVE"
    left = 70%
    top = 4%
    size = 12
    color = cyber-cyan
    pulse = true
    pulse-speed = 1200
}

# ---- Main Boot Menu ----
+ boot_menu {
    id = "boot_menu"
    left = 12%
    top = 20%
    width = 76%
    height = 60%

    item_font = "JetBrains Mono"
    item_color = "#a0a0b0"
    selected_item_color = chalk-white

    item_height = 46
    item_padding = 10
    item_spacing = 8

    # 9‑slice border for menu items (gear‑tooth style)
    item_pixmap_style = "assets/gear_slices/menu_*.png"
    selected_item_pixmap_style = "assets/gear_slices/menu_*.png"

    # Background of the menu box (semi‑transparent to show gears)
    menu_bg_color = menu-bg
    menu_bg_alpha = 0.7
}

# ---- Auto‑boot Timer (Progress Bar) ----
+ vbox {
    left = 20%
    top = 85%
    width = 60%
    height = 40

    + label {
        text = "AUTO-IGNITION IN"
        size = 11
        color = dim-gray
        align = "left"
    }

    + label {
        id = "timer_value"
        text = "30s"
        font = "Orbitron Bold"
        size = 18
        color = cyber-cyan
        align = "right"
    }

    + progress_bar {
        bar_style = "assets/progress_*.png"
        bar_bg_color = "#0a0a0a"
        bar_height = 4
        bar_width = 100%
        value = 100
    }
}

# ---- Footer Key Hints ----
+ hbox {
    left = 5%
    bottom = 3%
    width = 60%
    height = 20

    + label {
        text = "[↑↓] NAV   [↵] EXECUTE   [E] OVERRIDE   [Q] SCAN"
        size = 10
        color = dim-gray
    }
}

+ hbox {
    left = 72%
    bottom = 3%
    width = 25%
    height = 20

    + label {
        text = "GEAR RATIO: 0.9987   T: 15mK"
        size = 10
        color = quantum-purple
    }
}
